/* On scroll
======================================= */

/*
function onscrollfunc (point, myFunction) {
	
	$(document).ready(function(){
		$(window).scroll(
			function() {
			    if(($(this).scrollTop() > point)) {
					myFunction();
			    }
			}
		);
	});
}
*/

//onscrollfunc(100, stickyNav);

